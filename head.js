//connecting the assertEqual function
const assertEqual = require('./assertEqual');

// function to get the first item in an array
const head = function(array) {
  return array[0];
};

module.exports = head;
